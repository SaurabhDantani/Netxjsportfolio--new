/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['pbs.twimg.com','images.unsplash.com','unsplash.com','assets.aceternity.com'],
      },
};

module.exports = nextConfig;
